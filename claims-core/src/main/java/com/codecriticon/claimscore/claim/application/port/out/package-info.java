/**
 * Puertos de salida: contratos que la capa de aplicación necesita de infraestructura (persistencia,
 * publicación de eventos). Cada caso de uso depende solo de los puertos de salida que realmente
 * necesita.
 */

@NullMarked
package com.codecriticon.claimscore.claim.application.port.out;

import org.jspecify.annotations.NullMarked;
