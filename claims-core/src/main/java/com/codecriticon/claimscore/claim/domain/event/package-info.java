/**
 * Eventos de dominio emitidos por el agregado Claim (p. ej. una aprobación de siniestro). Tipos
 * inmutables, sin dependencia de infraestructura de mensajería — el mapeo a un evento de
 * integración vive en infrastructure.event.
 */

@NullMarked
package com.codecriticon.claimscore.claim.domain.event;

import org.jspecify.annotations.NullMarked;
