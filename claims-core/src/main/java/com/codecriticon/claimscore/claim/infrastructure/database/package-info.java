/**
 * Adaptador de persistencia: implementa los puertos de salida de application contra la base de
 * datos. Contiene entity (modelo de persistencia), mapper (entity↔domain) y repository
 * (implementación del puerto).
 */

@NullMarked
package com.codecriticon.claimscore.claim.infrastructure.database;

import org.jspecify.annotations.NullMarked;
