/**
 * Puertos de entrada de la capa de aplicación, divididos en command (escritura) y query (lectura).
 * Cada uno declara el contrato que implementan las clases de usecase.
 */

@NullMarked
package com.codecriticon.claimscore.claim.application.port.in;

import org.jspecify.annotations.NullMarked;
